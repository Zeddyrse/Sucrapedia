import { ResearchBonbonsPipe } from './research-bonbons.pipe';

describe('ResearchBonbonsPipe', () => {
  it('create an instance', () => {
    const pipe = new ResearchBonbonsPipe();
    expect(pipe).toBeTruthy();
  });
});
